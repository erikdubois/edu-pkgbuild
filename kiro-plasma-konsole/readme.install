post_install() {
    echo -e "\n** Kiro Konsole config installed to /etc/skel — new users get the Kiro profile as default. Existing users: cp -rT /etc/skel ~/ **\n"
}

post_upgrade() {
    post_install
}
